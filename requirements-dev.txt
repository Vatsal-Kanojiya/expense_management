# Development and CI dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Kept separate from requirements.txt so production images do not ship
# linters and test tooling.

# Linter + formatter, replaces black/flake8/isort.
ruff==0.14.2

# The [toml] extra is required for coverage to read pyproject.toml on
# Python < 3.11, which has no tomllib in the standard library.
coverage[toml]==7.11.0

# Runs the linters before each commit.
pre-commit==4.4.0
